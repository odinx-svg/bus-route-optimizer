---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tsconfig.json
  - frontend/tailwind.config.js
  - frontend/src/index.css
  - frontend/src/main.jsx
autonomous: true

must_haves:
  truths:
    - "TypeScript files (.tsx) compile without errors"
    - "Dark background (#0a0a0f) visible on page load"
    - "Inter font renders for all text"
    - "Tailwind neon color classes available (bg-neon-green, text-cyan-blue)"
  artifacts:
    - path: "frontend/tsconfig.json"
      provides: "TypeScript configuration with allowJs"
      contains: "allowJs"
    - path: "frontend/tailwind.config.js"
      provides: "Extended theme with neon colors and glass effects"
      contains: "neon-green"
    - path: "frontend/src/index.css"
      provides: "Dark theme body styles"
      contains: "bg-dark-bg"
    - path: "frontend/src/main.jsx"
      provides: "Inter font imports"
      contains: "@fontsource/inter"
  key_links:
    - from: "frontend/tsconfig.json"
      to: "frontend/src/**/*.tsx"
      via: "include directive"
      pattern: '"include".*"src"'
    - from: "frontend/tailwind.config.js"
      to: "frontend/src/index.css"
      via: "Tailwind directives process config"
      pattern: "@tailwind"
---

<objective>
Set up TypeScript, install dependencies, extend Tailwind with dark theme palette, and configure app shell with dark background and Inter font.

Purpose: Establish the foundation for all Phase 1 components - TypeScript for type safety, Tailwind theme for consistent styling, and dark theme as the visual base.
Output: Configured TypeScript, extended Tailwind theme, dark-themed app shell with Inter font loaded.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

# Existing files to modify
@frontend/package.json
@frontend/tailwind.config.js
@frontend/src/index.css
@frontend/src/main.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create tsconfig.json</name>
  <files>
    frontend/package.json
    frontend/tsconfig.json
  </files>
  <action>
    1. Navigate to frontend directory and install required packages:
       ```bash
       cd frontend && npm install typescript framer-motion @fontsource/inter
       ```

    2. Create `frontend/tsconfig.json` with the following configuration:
       - target: ES2020
       - module: ESNext
       - moduleResolution: bundler
       - allowJs: true (critical for mixed JS/TS codebase)
       - strict: true
       - noEmit: true (Vite handles transpilation)
       - jsx: react-jsx
       - isolatedModules: true (required by Vite/esbuild)
       - include: ["src"]
       - references: [{ "path": "./tsconfig.node.json" }]

    Do NOT enable checkJs (too noisy during migration).
    Use exact config from 01-RESEARCH.md "tsconfig.json for Mixed JS/TS Vite Project" section.
  </action>
  <verify>
    - Run `npm ls typescript framer-motion @fontsource/inter` to confirm packages installed
    - Run `npx tsc --noEmit` in frontend directory - should complete without errors
  </verify>
  <done>
    - package.json includes typescript, framer-motion, @fontsource/inter
    - tsconfig.json exists with allowJs: true and strict: true
    - TypeScript compiles existing JS files without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Tailwind config with dark theme palette</name>
  <files>
    frontend/tailwind.config.js
  </files>
  <action>
    Update `frontend/tailwind.config.js` to ADD (not replace) the following extensions:

    1. Colors (extend existing):
       - 'neon-green': '#39FF14'
       - 'cyan-blue': '#00D4FF'
       - 'dark-bg': '#0a0a0f'
       - 'glass': { DEFAULT: 'rgba(255, 255, 255, 0.1)', 'border': 'rgba(255, 255, 255, 0.2)' }

    2. Font family (update existing):
       - sans: ['Inter', 'system-ui', 'sans-serif']

    3. Box shadows (add new):
       - 'neon-green': '0 0 5px rgba(57, 255, 20, 0.5), 0 0 20px rgba(57, 255, 20, 0.3)'
       - 'neon-green-lg': '0 0 10px rgba(57, 255, 20, 0.6), 0 0 40px rgba(57, 255, 20, 0.4)'
       - 'neon-cyan': '0 0 5px rgba(0, 212, 255, 0.5), 0 0 20px rgba(0, 212, 255, 0.3)'
       - 'neon-cyan-lg': '0 0 10px rgba(0, 212, 255, 0.6), 0 0 40px rgba(0, 212, 255, 0.4)'
       - 'glass': '0 8px 32px rgba(0, 0, 0, 0.3)'

    4. Backdrop blur (add new):
       - 'glass': '12px'

    IMPORTANT: Use `extend` pattern to ADD colors, not replace the entire colors object.
    Keep existing primary, secondary, dark, light colors.
  </action>
  <verify>
    - Run `npm run build` in frontend - build should succeed
    - Inspect built CSS or run dev server, verify `bg-neon-green` class exists
  </verify>
  <done>
    - tailwind.config.js contains neon-green, cyan-blue, dark-bg colors
    - Box shadows neon-green, neon-cyan, glass defined
    - Backdrop blur glass defined
    - Build succeeds without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Update app shell with dark theme and Inter font</name>
  <files>
    frontend/src/index.css
    frontend/src/main.jsx
  </files>
  <action>
    1. Update `frontend/src/main.jsx`:
       - Add Inter font imports BEFORE the App import:
         ```jsx
         import '@fontsource/inter/400.css'
         import '@fontsource/inter/500.css'
         import '@fontsource/inter/600.css'
         import '@fontsource/inter/700.css'
         ```
       - Keep existing imports and render logic unchanged

    2. Update `frontend/src/index.css`:
       - Change body styles from `bg-light text-dark` to `bg-dark-bg text-white`
       - Add `antialiased` for crisp font rendering
       - Result: `@apply bg-dark-bg text-white font-sans antialiased;`

    Note: The existing App.jsx already uses dark colors (bg-slate-900), so the visual change will be subtle. The key is establishing the dark-bg as the base.
  </action>
  <verify>
    - Run `npm run dev` in frontend
    - Open http://localhost:5173 in browser
    - Body background should be #0a0a0f (very dark, almost black)
    - Text should render in Inter font (compare to system font)
    - DevTools: Inspect body, confirm `background-color: rgb(10, 10, 15)`
  </verify>
  <done>
    - main.jsx imports @fontsource/inter CSS files
    - index.css body uses bg-dark-bg and text-white
    - Page renders with dark background and Inter font
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. TypeScript check:
   ```bash
   cd frontend && npx tsc --noEmit
   ```
   Should complete with no errors.

2. Build check:
   ```bash
   cd frontend && npm run build
   ```
   Should complete successfully.

3. Visual check:
   - Run `npm run dev`
   - Open browser to http://localhost:5173
   - Background is very dark (#0a0a0f)
   - Text uses Inter font
   - Existing functionality (upload, optimize) still works
</verification>

<success_criteria>
- tsconfig.json configured with allowJs: true
- Tailwind config extended with neon/glass color palette
- Dark background (#0a0a0f) applied to app shell
- Typography uses Inter font
- Build and TypeScript checks pass
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
