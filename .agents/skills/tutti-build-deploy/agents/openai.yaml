interface:
  display_name: "Tutti Build & Deploy"
  short_description: "Desktop build, release y deployment"
  brand_color: "#374151"
  default_prompt: "Use $tutti-build-deploy to revisar build desktop, releases o deployment de Tutti."

policy:
  allow_implicit_invocation: true
