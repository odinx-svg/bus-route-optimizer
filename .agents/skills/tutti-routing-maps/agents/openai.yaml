interface:
  display_name: "Tutti Routing & Maps"
  short_description: "OSRM, geometria y visualizacion en mapa"
  brand_color: "#0891B2"
  default_prompt: "Use $tutti-routing-maps to revisar OSRM, cache, geometrias y componentes de mapa de Tutti."

policy:
  allow_implicit_invocation: true
